# 类型系统 · 渐进式披露子项（references）

> 本目录承载 [004 类型系统(../../004-type-system.md) 的**能力子项**。004 主文档保留架构级表述；深度设计、契约细节下沉至此，按需钻取。**一子项一文档，互不重叠**；子项仅补细节，不与主文档重复表述既有决策。

| 子项 | 内容 | 关联主文档章节 |
|------|------|---------------|
| [值类型视图 ABI（装箱 / 接口 / 可空 / enum）](value-type-view-abi.md) | 装箱视图 box 布局、接口视图约束调用 vs 装箱调用、可空视图内联布局与装箱恒等式、enum 值语义哈希、统一发射管线、Copy 交互、现状核对与实现分解 | 004 §值类型视图 ABI |
| [枚举能力增强（位运算 · Flags 特性 · 编译期工具方法）](enum-capabilities.md) | 枚举位运算 `\|` `&` `^` `~`、复合赋值 `\|=` `&=` `^=`、`[Flags]` 特性、`Enum.HasFlag`/`IsDefined`/`GetNames`/`GetValues` 编译期烘焙 | 004 §命名类型 enum / §enum 哈希相等 |

---

[返回 004 主题入口(../../004-type-system.md) · [返回 RFC 索引](../../index.md)
