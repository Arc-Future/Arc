# RFC 001 语言宪章

## 背景

Arc 是一门**编译型系统编程语言**：以 C# 表面语法为基准，融合 Rust 风格的内存安全语义，经 AOT 编译为原生机器码，拒绝全局停顿式垃圾回收。本宪章是语言设计的**北极星**——后续所有设计决策必须可追溯到本宪章的某条信条。

## 设计决策

### 四元设计方程

```
Arc = 可读性 × 编译期安全 × AOT 确定性 × 人机协作性
```

四个因子**相乘**而非相加——任何一项为零，语言价值归零。任何新特性必须同时满足四个维度，缺一即不采纳。

### 五条设计信条

| 信条 | 含义 | 核心机制 |
|------|------|---------|
| ① 可读即可协作 | 程序首先是写给人与智能体共同阅读的文本 | 前导类型、单一惯用法、确定性格式、声明式查询 |
| ② 安全在编译期完成 | 运行时崩溃是设计的失败，不是程序的常态 | 资源所有权、借用约束、穷尽匹配、显式错误链 |
| ③ 行为在编译期确定 | 可预测性是系统软件的底线 | 纯 AOT、无 STW GC、零成本抽象、确定性输出 |
| ④ 代码即数据 | 程序结构是可分析、可变换、可传递的数据 | 表达式树、Provider 模式、双路径查询 |
| ⑤ 为人机共写而生 | 智能体是一等协作伙伴 | 结构化诊断、局部推理、能力显式、契约可声明 |

### 语言边界

| 拒绝 | 追求 |
|------|------|
| 隐式魔法与不可预测的运行时行为 | 人类读得自然、智能体写得准确、编译器验得严格、机器跑得快 |
| 以运行时反射替代编译期验证 | 成为智能时代系统编程的公共协议 |
| 多种等价写法并存的语法冗余 | 单一惯用法——每种意图只有一种推荐表达 |
| 将智能体适配视为事后补丁而非设计原点 | 智能体从设计原点即为一等协作伙伴 |

### 非目标

- 自然语言编程
- 运行时 Agent hook 内嵌于语言语义
- 应用侧 Agent / RAG / 编排框架（与「写 Arc 的协作 Agent」正交）
- C# 完备对等（Arc 取精华、去糟粕，非对等替代）

## 边界

- 具体语法与编码决定见 [语法表面与编码标准](002-surface-contract.md)及[词法与语法](003-lexicon-syntax.md)。
- 类型体系见 [类型系统](004-type-system.md)。
- 内存与资源语义见 [内存模型与资源安全](005-memory-model.md)。

---

上一节：[RFC 索引](index.md) · 下一节：[002 语法表面与编码标准](002-surface-contract.md)