# 017 编译产物与包体系 · 渐进式披露子项（references）

> 本目录承载 [017 编译产物、包体系与类型身份(../../017-build-artifacts-packages.md) 的**能力子项**。017 主文档保留架构级表述；深度设计、契约细节下沉至此，按需钻取。**一子项一文档，互不重叠**；子项仅补细节，不与主文档重复表述既有决策。

| 子项 | 内容 | 关联主文档章节 |
|------|------|---------------|
| [SDK 布局与资源自定位](sdk-layout.md) | SDK 目录布局契约（`bin/` + `lib/{std,rt,native}`）、双布局等价识别、`current_exe()` 自定位、std 解析链、`rt_cache` 用户缓存 | 017 背景 · 设计决策 · 边界 |

---

[返回 017 主题入口(../../017-build-artifacts-packages.md) · [返回 RFC 索引](../../index.md)
