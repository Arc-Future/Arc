[package]
name = "arc_rust-hotpath"
version = "0.1.0"
edition = "2021"

# 独立于 Arc 主 workspace（性能对照 harness，非编译器成员）。
[workspace]

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
