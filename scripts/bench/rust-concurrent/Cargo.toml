[package]
name = "arc_rust-concurrent"
version = "0.1.0"
edition = "2021"

# 独立于 Arc 主 workspace（RFC 015 并发性能协议对照 harness）。
[workspace]

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
