// RFC 103 运行时库加载统一模型（`load` 声明）auto 策略 e2e 测试辅助契约。
//
// `load = "auto"`：编译期可定位（符号验证成功）→ static；否则降级 runtime。
// 本契约的库目录在测试时通常**不存在**（可选组件缺失场景）——auto 必须
// 确定性降级 runtime，不得造成编译期硬失败。`library` 相对路径基准 = 执行
// 程序根目录（`-o` 输出可执行文件所在目录）：e2e exe 位于 `target/e2e/<app>.exe`
// → 库目录解析为 `target/e2e/ani_auto_module/lib`。
native module ani_auto_module {
    load = "auto";
    library = "ani_auto_module/lib";
    fn ani_auto_value() -> int;
}
