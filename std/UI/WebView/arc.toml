[package]
name = "Arc.UI.WebView"
edition = "1"
version = "1.0.0"
kind = "library"
namespace = "Arc.UI.WebView"

# 浏览器组件库：系统 WebView（WebView2 / WKWebView / WebKitGTK）经 GPU 零拷贝
# 捕获进 wgpu 合成面 + 注入脚本/自定义协议桥。设计权威：
#   docs/rfc/037-ui/references/webview-surface.md
# 实现未开工（单 M · 子集边界 · 不自动开干）；本库当前仅注册命名空间与依赖拓扑。

[dependencies]
Arc = { path = "../../Arc" }
"Arc.UI" = { path = "../Core" }
