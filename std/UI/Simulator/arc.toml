[package]
name = "Arc.UI.Simulator"
edition = "1"
version = "1.0.0"
kind = "library"
namespace = "Arc.UI.Simulator"

# 模拟器承载组件库：模拟器界面集成（如基于 Android 模拟器核心的屏幕集成）。
# 渲染落点：Arc.UI 纹理表面（VideoSurface + DrawTexture，RFC 037 references/
# texture-surface）承载 guest 帧缓冲；输入注入/内核集成列后续子项。
# 实现未开工（单 M · 子集边界 · 不自动开干）；本库当前仅注册命名空间与依赖拓扑。

[dependencies]
Arc = { path = "../../Arc" }
"Arc.UI" = { path = "../Core" }
